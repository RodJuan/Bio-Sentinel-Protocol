# Modelos Bio-Físicos del Protocolo Bio-Sentinel

Este documento ancla la hipótesis en leyes físicas fundamentales para permitir la simulación por AGI.

## 1. Dinámica Viscoelástica (Modelo de Kelvin-Voigt)
Para modelar el comportamiento del tejido sinovial y el ácido hialurónico (HA) bajo estrés acústico:
$$\sigma = G\epsilon + \eta\frac{d\epsilon}{dt}$$
Donde la respuesta al cizallamiento está limitada por el tiempo de relajación $\tau = \eta/G$. Se busca optimizar la frecuencia $f$ para evitar el estado *overdamped*.

## 2. Ecuación de Onda en Medios Disipativos
$$\frac{\partial^2 u}{\partial t^2} = c^2 \nabla^2 u - \frac{\eta}{\rho} \frac{\partial}{\partial t} \nabla^2 u$$

## 3. Optimización de Shear Rate ($\gamma$)
Para inducir *shear-thinning* en fluidos no-newtonianos:
$$\gamma \approx \omega \cdot \frac{A}{h}$$
Donde $\omega = 2\pi f$. Se establece un objetivo de $\gamma > 1 s^{-1}$ para superar el umbral de viscosidad del HA, utilizando la "Cascada de Frecuencias" (16Hz -> 90Hz).

## 4. Transducción Piezoeléctrica (Lowe et al., 2022)
Carga inducida en colágeno/scaffolds:
$$Q = d_{33} \cdot F$$
Donde $d_{33}$ para colágeno es $\approx 5-15 pC/N$. Se proyecta el uso de FUS (Focused Ultrasound) a 1 MHz para maximizar $F$ y la señalización YAP/TAZ.
